@model List<WSAD_App1.Areas.Admin.Models.ViewModels.ManageUser.ManageUserViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Manage Users</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@using (Html.BeginForm("Delete", "ManageUser"))
{
    <button type="submit" class="btn btn-danger">Delete Users</button>

<table class="table">
    <tr>
        <th>&nbsp;</th>
        <th>
            First Name
        </th>
        <th>
            Last Name
        </th>
        <th>
            Email Address
        </th>
        <th>
            Username
        </th>
        <th>
            Is Active
        </th>
        <th>
            Is Admin
        </th>
        <th>
            Gender
        </th>
        <th>
            DateCreated
        </th>
        <th></th>
    </tr>

@*foreach (var item in Model)*@
@for(int i=0; i<Model.Count; i++)
{
    var item = Model[i];
    <tr>
        <td>
            @Html.HiddenFor(x => Model[i].Id)
            @Html.CheckBoxFor(x => Model[i].IsSelected)
        </td>
        <td>
            @item.FirstName
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmailAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @item.IsActive
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsAdmin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gender)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateCreated)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", "Account", new { /* id=item.Id */ }, null) |
            @Html.ActionLink("Details", "UserProfile", "Account", new { /* id=item.Id */ }, null) |
            @Html.ActionLink("Delete", "Delete", "Account", new { /* id=item.Id */ }, null)
        </td>
    </tr>
}

</table>
    <button type="submit" class="btn btn-danger">Delete Users</button>
}
